<UserControl x:Class="R3.Desktop.Views.CashTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:CashTransactionsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <TextBlock DockPanel.Dock="Top" Text="Kasa Hareketleri" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12"/>
        <Border DockPanel.Dock="Top" Background="#F6F6F6" BorderBrush="#DEDEDE" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10">
            <WrapPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,10,0"><TextBlock Text="Kasa" FontSize="10" Foreground="#848484"/><ComboBox Width="180" ItemsSource="{Binding CashAccounts}" DisplayMemberPath="Name" SelectedValuePath="Id" SelectedValue="{Binding SelectedCashAccountId}" Padding="7"/></StackPanel>
                <StackPanel Margin="0,0,10,0"><TextBlock Text="Başlangıç" FontSize="10" Foreground="#848484"/><DatePicker Width="130" SelectedDate="{Binding FromDate}"/></StackPanel>
                <StackPanel Margin="0,0,10,0"><TextBlock Text="Bitiş" FontSize="10" Foreground="#848484"/><DatePicker Width="130" SelectedDate="{Binding ToDate}"/></StackPanel>
                <StackPanel Margin="0,0,10,0"><TextBlock Text="İşlem Tipi" FontSize="10" Foreground="#848484"/><ComboBox Width="160" ItemsSource="{Binding TransactionTypes}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding TransactionType}" Padding="7"/></StackPanel>
                <StackPanel Margin="0,0,10,0"><TextBlock Text="Giriş / Çıkış" FontSize="10" Foreground="#848484"/><ComboBox Width="100" ItemsSource="{Binding Directions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Direction}" Padding="7"/></StackPanel>
                <ui:Button Content="Filtrele" Command="{Binding RefreshCommand}" Padding="12,7" Appearance="Primary" VerticalAlignment="Bottom" Margin="0,0,8,0"/>
                <ui:Button Content="Hareketi İptal Et" Click="Reverse_Click" Padding="12,7" VerticalAlignment="Bottom"/>
            </WrapPanel>
        </Border>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Foreground="Firebrick" Margin="0,8,0,0"/>
        <DataGrid Style="{StaticResource ProfessionalDataGridStyle}" ItemsSource="{Binding Transactions}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Date}" Width="150"/>
                <DataGridTextColumn Header="Kasa" Binding="{Binding CashAccount}" Width="130"/>
                <DataGridTextColumn Header="İşlem Tipi" Binding="{Binding TransactionType}" Width="130"/>
                <DataGridTextColumn Header="Belge" Binding="{Binding Document}" Width="110"/>
                <DataGridTextColumn Header="Cari" Binding="{Binding Account}" Width="140"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Giriş" Binding="{Binding In, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Çıkış" Binding="{Binding Out, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Döviz" Binding="{Binding Currency}" Width="70"/>
                <DataGridTextColumn Header="Kullanıcı" Binding="{Binding User}" Width="100"/>
                <DataGridTextColumn Header="Durum" Binding="{Binding Status}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
