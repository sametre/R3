<UserControl x:Class="R3.Desktop.Views.AccountTransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AccountLedgerViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <!-- The toolbar button below says "(F5)" but nothing made the key itself do anything until
         this binding was added - pressing F5 just typed nowhere. -->
    <UserControl.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}" />
    </UserControl.InputBindings>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Cari Hareketler" FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <ui:Button Content="Yenile (F5)" Command="{Binding RefreshCommand}" Padding="10,5"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Foreground="Firebrick" Margin="0,8,0,0"/>
        <DataGrid Style="{StaticResource ProfessionalDataGridStyle}" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Date}" Width="150"/>
                <DataGridTextColumn Header="Cari Kodu" Binding="{Binding AccountCode}" Width="110"/>
                <DataGridTextColumn Header="Cari Adı" Binding="{Binding AccountName}" Width="2*"/>
                <DataGridTextColumn Header="İşlem" Binding="{Binding TransactionType}" Width="110"/>
                <DataGridTextColumn Header="Belge No" Binding="{Binding DocumentNo}" Width="120"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Borç" Binding="{Binding Debit, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Alacak" Binding="{Binding Credit, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Döviz" Binding="{Binding Currency}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
