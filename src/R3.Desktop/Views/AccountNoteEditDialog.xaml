<Window x:Class="R3.Desktop.Views.AccountNoteEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AccountNoteEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="Not Ekle" Width="480" SizeToContent="Height" MinWidth="420" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Margin="18">
        <TextBlock Text="Not Ekle" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
        <TextBlock Text="Not Tipi *" Margin="0,0,0,5"/>
        <ComboBox ItemsSource="{Binding NoteTypes}" SelectedItem="{Binding NoteType}" Padding="7"/>
        <TextBlock Text="Başlık *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" MaxLength="200"/>
        <TextBlock Text="Not *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="110"/>
        <CheckBox Content="Sabit (üstte göster)" IsChecked="{Binding IsPinned}" Margin="0,12,0,0"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Vazgeç" IsCancel="True" Width="78" Height="30" Margin="0,0,7,0"/>
            <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Width="90" Height="30" Appearance="Primary" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
