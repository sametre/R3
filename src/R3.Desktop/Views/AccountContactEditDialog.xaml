<Window x:Class="R3.Desktop.Views.AccountContactEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AccountContactEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding Title}" Width="440" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Margin="24">
        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><TextBlock Text="Ad *"/><ui:TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" Margin="0,5,0,0"/></StackPanel>
            <StackPanel Grid.Column="2"><TextBlock Text="Soyad"/><ui:TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" Margin="0,5,0,0"/></StackPanel>
        </Grid>

        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><TextBlock Text="Görev"/><ui:TextBox Text="{Binding JobTitle, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" Margin="0,5,0,0"/></StackPanel>
            <StackPanel Grid.Column="2"><TextBlock Text="Departman"/><ui:TextBox Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}" MaxLength="100" Margin="0,5,0,0"/></StackPanel>
        </Grid>

        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><TextBlock Text="Telefon"/><ui:TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" Margin="0,5,0,0"/></StackPanel>
            <StackPanel Grid.Column="2"><TextBlock Text="Cep Telefonu"/><ui:TextBox Text="{Binding MobilePhone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" Margin="0,5,0,0"/></StackPanel>
        </Grid>

        <TextBlock Text="E-posta" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="160" />

        <TextBlock Text="Notlar" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" MaxLength="500" Height="60" AcceptsReturn="True" TextWrapping="Wrap" />

        <CheckBox Content="Ana yetkili" IsChecked="{Binding IsPrimary}" Margin="0,14,0,0" />

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,12,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Vazgeç" IsCancel="True" Padding="18,9" Margin="0,0,8,0" />
            <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Padding="18,9" Appearance="Primary" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
