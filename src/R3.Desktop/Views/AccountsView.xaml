<UserControl x:Class="R3.Desktop.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AccountsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,14">
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Foreground="#243B53" />
            <TextBlock Text="{Binding Subtitle}" Foreground="#718096" Margin="0,3,0,0" />
        </StackPanel>
        <WrapPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,12">
            <ui:Button x:Name="NewButton" Content="+ Yeni Cari (F2)" Click="NewButton_Click" Margin="0,0,8,0" Padding="14,8" Appearance="Primary" />
            <ui:Button x:Name="EditButton" Content="Düzenle (F3)" Click="EditButton_Click" Margin="0,0,8,0" Padding="14,8" />
            <ui:Button Content="Yenile (F5)" Command="{Binding RefreshCommand}" Margin="0,0,16,0" Padding="14,8" />
            <TextBlock Text="Ara: " VerticalAlignment="Center" />
            <ui:TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="260"
                        PlaceholderText="Cari kodu, adı, vergi no, telefon veya e-posta" />
        </WrapPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Foreground="Firebrick" Margin="0,8,0,0" />
        <DataGrid x:Name="Grid" ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" BorderBrush="LightGray" RowHeight="34" ColumnHeaderHeight="36"
                  MouseDoubleClick="Grid_MouseDoubleClick" PreviewMouseRightButtonDown="Grid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Yeni cari" Click="ContextNew_Click" />
                    <MenuItem Header="Seçili cariyi düzenle" Click="ContextEdit_Click" />
                    <Separator />
                    <MenuItem Header="Cari kodunu kopyala" Click="ContextCopyCode_Click" />
                    <MenuItem Header="Listeyi yenile" Click="ContextRefresh_Click" />
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cari Kodu" Binding="{Binding Code}" Width="110" />
                <DataGridTextColumn Header="Cari Adı" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Tip" Binding="{Binding AccountType}" Width="140" />
                <DataGridTextColumn Header="Vergi No / TCKN" Binding="{Binding TaxNumber}" Width="130" />
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120" />
                <DataGridTextColumn Header="Borç" Binding="{Binding Debit, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="Alacak" Binding="{Binding Credit, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="Bakiye" Binding="{Binding Balance, StringFormat=N2}" Width="100" />
                <DataGridTextColumn Header="Durum" Binding="{Binding BalanceStatus}" Width="90" />
                <DataGridTextColumn Header="Kredi Limiti" Binding="{Binding CreditLimit, StringFormat=N2}" Width="110" />
                <DataGridTextColumn Header="Kullanılabilir Limit" Binding="{Binding AvailableCredit, StringFormat=N2}" Width="130" />
                <DataGridCheckBoxColumn Header="Aktif" Binding="{Binding IsActive, Mode=OneWay}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
