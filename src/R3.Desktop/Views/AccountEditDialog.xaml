<Window x:Class="R3.Desktop.Views.AccountEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AccountEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding Title}" Width="480" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Margin="24">
        <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12" />

        <TextBlock Text="Cari Kodu *" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" />

        <TextBlock Text="Cari Adı *" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="160" />

        <TextBlock Text="Cari Tipi *" Margin="0,10,0,5" />
        <ComboBox ItemsSource="{Binding AccountTypes}" SelectedItem="{Binding AccountType}" Padding="8" />

        <TextBlock Text="Vergi Dairesi" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding TaxOffice, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" />

        <TextBlock Text="Vergi No / TCKN" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding TaxNumber, UpdateSourceTrigger=PropertyChanged}" MaxLength="11" />

        <TextBlock Text="Telefon" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" />

        <TextBlock Text="Cep Telefonu" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding MobilePhone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30" />

        <TextBlock Text="E-posta" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="160" />

        <TextBlock Text="Kredi Limiti" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding CreditLimit, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Risk Limiti" Margin="0,10,0,5" />
        <ui:TextBox Text="{Binding RiskLimit, UpdateSourceTrigger=PropertyChanged}" />

        <CheckBox Content="Aktif" IsChecked="{Binding IsActive}" Margin="0,14,0,0" />

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,12,0,0" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Vazgeç" IsCancel="True" Padding="18,9" Margin="0,0,8,0" />
            <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Padding="18,9" Appearance="Primary" IsDefault="True" />
        </StackPanel>
    </StackPanel>
</Window>
