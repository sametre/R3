<Window x:Class="R3.Desktop.Views.AccountEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:AccountEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding Title}" Width="920" Height="720" MinWidth="760" MinHeight="600" ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Margin="22">
        <Border DockPanel.Dock="Top" Background="#F3F8FC" BorderBrush="#D8E4F0" BorderThickness="1" CornerRadius="9" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock FontSize="21" FontWeight="SemiBold"><Run Text="{Binding Code}"/><Run Text="   "/><Run Text="{Binding Name}"/></TextBlock>
                    <StackPanel Orientation="Horizontal" Margin="0,7,0,0"><TextBlock Text="{Binding AccountType}" Foreground="#52667A"/><TextBlock Text="  •  "/><TextBlock Text="{Binding BalanceStatus}" Foreground="#2A9D8F" FontWeight="SemiBold"/></StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1"><TextBlock Text="Bakiye" Foreground="#718096"/><TextBlock Text="{Binding Balance, StringFormat=N2}" FontSize="19" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="Kredi Limiti" Foreground="#718096"/><TextBlock Text="{Binding CreditLimit, StringFormat=N2}" FontSize="19" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel>
                <StackPanel Grid.Column="3"><TextBlock Text="Kullanılabilir" Foreground="#718096"/><TextBlock Text="{Binding AvailableCredit, StringFormat=N2}" FontSize="19" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel>
            </Grid>
        </Border>

        <StackPanel DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Vazgeç" IsCancel="True" Padding="18,9" Margin="0,0,8,0" />
                <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Padding="18,9" Appearance="Primary" IsDefault="True" />
            </StackPanel>
        </StackPanel>

        <TabControl>
            <TabItem Header="Genel">
                <ScrollViewer VerticalScrollBarVisibility="Auto"><Grid Margin="18">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Kimlik" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock Text="Cari Kodu *" Margin="0,8,0,5"/><ui:TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" MaxLength="30"/>
                        <TextBlock Text="Cari Adı *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="160"/>
                        <TextBlock Text="Cari Tipi *" Margin="0,10,0,5"/><ComboBox ItemsSource="{Binding AccountTypes}" SelectedItem="{Binding AccountType}" Padding="8"/>
                        <CheckBox Content="Aktif" IsChecked="{Binding IsActive}" Margin="0,14,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock Text="Vergi Bilgileri" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <TextBlock Text="Vergi Dairesi" Margin="0,8,0,5"/><ui:TextBox Text="{Binding TaxOffice, UpdateSourceTrigger=PropertyChanged}" MaxLength="80"/>
                        <TextBlock Text="Vergi No / TCKN" Margin="0,10,0,5"/><ui:TextBox Text="{Binding TaxNumber, UpdateSourceTrigger=PropertyChanged}" MaxLength="11"/>
                    </StackPanel>
                </Grid></ScrollViewer>
            </TabItem>
            <TabItem Header="İletişim &amp; Adres">
                <StackPanel Margin="18" Width="480" HorizontalAlignment="Left">
                    <TextBlock Text="İletişim" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="Telefon" Margin="0,12,0,5"/><ui:TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30"/>
                    <TextBlock Text="Cep Telefonu" Margin="0,10,0,5"/><ui:TextBox Text="{Binding MobilePhone, UpdateSourceTrigger=PropertyChanged}" MaxLength="30"/>
                    <TextBlock Text="E-posta" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" MaxLength="160"/>
                    <TextBlock Text="Çoklu adres ve yetkili kayıtları bir sonraki şema adımında bu sekmeye bağlanacaktır." Foreground="#718096" TextWrapping="Wrap" Margin="0,22,0,0"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="Finans">
                <StackPanel Margin="18" Width="480" HorizontalAlignment="Left">
                    <TextBlock Text="Risk ve Limitler" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="Kredi Limiti" Margin="0,12,0,5"/><ui:TextBox Text="{Binding CreditLimit, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Risk Limiti" Margin="0,10,0,5"/><ui:TextBox Text="{Binding RiskLimit, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="Finansal bakiye AccountBalance projeksiyonundan okunur; bu ekranda yeniden hesaplanmaz." Foreground="#718096" TextWrapping="Wrap" Margin="0,22,0,0"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="E-Belge"><TextBlock Text="E-Fatura ve E-İrsaliye profilleri için veri modeli hazırlanıyor." Foreground="#718096" Margin="20"/></TabItem>
            <TabItem Header="Cari Hareketler"><TextBlock Text="Cari hareketler menüsündeki ekstre ekranından görüntülenir." Foreground="#718096" Margin="20"/></TabItem>
            <TabItem Header="Geçmiş"><TextBlock Text="Audit kayıtları bu sekmeye bağlanacaktır." Foreground="#718096" Margin="20"/></TabItem>
        </TabControl>
    </DockPanel>
</Window>
