<UserControl x:Class="R3.Desktop.Views.AccountStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AccountStatementViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel Margin="18">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock Text="Cari Ekstre" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Cari: " VerticalAlignment="Center"/>
                <ComboBox Width="340" DisplayMemberPath="Name" ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}" Padding="7"/>
            </StackPanel>
        </StackPanel>
        <Border DockPanel.Dock="Top" Background="#F3F5F6" BorderBrush="#D0D5D9" BorderThickness="1" CornerRadius="7" Padding="12" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,26,0"><TextBlock Text="Toplam Borç" Foreground="#718096" FontSize="10"/><TextBlock Text="{Binding TotalDebit, StringFormat=N2}" FontSize="16" FontWeight="SemiBold"/></StackPanel>
                <StackPanel Margin="0,0,26,0"><TextBlock Text="Toplam Alacak" Foreground="#718096" FontSize="10"/><TextBlock Text="{Binding TotalCredit, StringFormat=N2}" FontSize="16" FontWeight="SemiBold"/></StackPanel>
                <StackPanel><TextBlock Text="Kapanış Bakiye" Foreground="#718096" FontSize="10"/><TextBlock Text="{Binding ClosingBalance, StringFormat=N2}" FontSize="16" FontWeight="SemiBold" Foreground="#2A9D8F"/></StackPanel>
            </StackPanel>
        </Border>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Foreground="Firebrick" Margin="0,8,0,0"/>
        <DataGrid Style="{StaticResource ProfessionalDataGridStyle}" ItemsSource="{Binding Lines}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Date}" Width="150"/>
                <DataGridTextColumn Header="Belge" Binding="{Binding DocumentNo}" Width="120"/>
                <DataGridTextColumn Header="İşlem" Binding="{Binding TransactionType}" Width="110"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Borç" Binding="{Binding Debit, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Alacak" Binding="{Binding Credit, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Bakiye" Binding="{Binding Balance, StringFormat=N2}" Width="100"/>
                <DataGridTextColumn Header="Döviz" Binding="{Binding Currency}" Width="70"/>
                <DataGridTextColumn Header="Kur" Binding="{Binding ExchangeRate, StringFormat=N2}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
