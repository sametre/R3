<Window x:Class="R3.Desktop.Views.ManualLedgerEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:ManualLedgerEntryViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="Cari Borç / Alacak İşlemi" Width="520" SizeToContent="Height" MinWidth="460" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Margin="18">
        <TextBlock Text="Cari Borç / Alacak İşlemi" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Text="{Binding AccountName}" Foreground="#848484" Margin="0,0,0,10"/>
        <Border Background="#FFF6E5" BorderBrush="#F0D48A" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,12">
            <TextBlock Text="Manuel cari hareketleri finansal bakiyeyi doğrudan etkiler." TextWrapping="Wrap" Foreground="#8A6D1F"/>
        </Border>
        <TextBlock Text="İşlem Tipi *" Margin="0,0,0,5"/>
        <ComboBox ItemsSource="{Binding Directions}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Direction}" Padding="7"/>
        <TextBlock Text="Tutar *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,8,0"><TextBlock Text="Döviz" Margin="0,0,0,5"/><ui:TextBox Text="{Binding CurrencyCode, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
            <StackPanel Grid.Column="1"><TextBlock Text="Kur" Margin="0,0,0,5"/><ui:TextBox Text="{Binding ExchangeRate, UpdateSourceTrigger=PropertyChanged}"/></StackPanel>
        </Grid>
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"><TextBlock Text="Tarih *" Margin="0,0,0,5"/><DatePicker SelectedDate="{Binding Date}"/></StackPanel>
            <StackPanel Grid.Column="2"><TextBlock Text="Vade" Margin="0,0,0,5"/><DatePicker SelectedDate="{Binding DueDate}"/></StackPanel>
        </Grid>
        <TextBlock Text="Belge No" Margin="0,10,0,5"/><ui:TextBox Text="{Binding DocumentNo, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Açıklama *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="60"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,10,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Vazgeç" IsCancel="True" Width="78" Height="30" Margin="0,0,7,0"/>
            <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Width="90" Height="30" Appearance="Primary" IsDefault="True"/>
        </StackPanel>
    </StackPanel>
</Window>
