<Window x:Class="R3.Desktop.Views.ReceiptPaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:ReceiptPaymentViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding Title}" Width="620" SizeToContent="Height" MinWidth="560" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Margin="18">
        <StackPanel DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Vazgeç" IsCancel="True" Width="78" Height="30" Margin="0,0,7,0"/>
                <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Width="90" Height="30" Appearance="Primary" IsDefault="True"/>
            </StackPanel>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,16,0">
                <TextBlock Text="{Binding Title}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding AccountName}" Foreground="{StaticResource R3.Text.Secondary.Brush}" Margin="0,0,0,14"/>

                <TextBlock Text="Tarih *" Margin="0,0,0,5"/><DatePicker SelectedDate="{Binding Date}"/>
                <TextBlock Text="Tutar *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="120"/></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0"><TextBlock Text="Para Birimi *" Margin="0,0,0,5"/><ComboBox ItemsSource="{Binding Currencies}" Text="{Binding CurrencyCode, UpdateSourceTrigger=PropertyChanged}" IsEditable="True" Padding="7"/></StackPanel>
                    <StackPanel Grid.Column="1"><TextBlock Text="Kur" Margin="0,0,0,5"/><ui:TextBox Text="{Binding ExchangeRate, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsForeignCurrency}"/></StackPanel>
                </Grid>
                <TextBlock Text="Ödeme Yöntemi *" Margin="0,10,0,5"/>
                <ComboBox ItemsSource="{Binding PaymentMethods}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding PaymentMethod}" Padding="7"/>
                <TextBlock Text="Belge No" Margin="0,10,0,5"/><ui:TextBox Text="{Binding DocumentNo, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="Açıklama" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="60"/>
            </StackPanel>

            <Border Grid.Column="1" Background="{StaticResource R3.Surface.Alt.Brush}" BorderBrush="{StaticResource R3.Border.Brush}" BorderThickness="1" CornerRadius="7" Padding="14" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Mevcut Bakiye" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/>
                    <TextBlock Text="{Binding CurrentBalance, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold" Margin="0,2,0,12"/>
                    <TextBlock Text="Tutar" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/>
                    <TextBlock Text="{Binding Amount, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold" Margin="0,2,0,12"/>
                    <Separator Margin="0,0,0,12"/>
                    <TextBlock Text="Yeni Bakiye" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/>
                    <TextBlock Text="{Binding NewBalance, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="Bold" Foreground="{StaticResource R3.Accent.Brush}" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
