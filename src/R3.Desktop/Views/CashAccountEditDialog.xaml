<Window x:Class="R3.Desktop.Views.CashAccountEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:R3.Desktop.ViewModels"
        xmlns:local="clr-namespace:R3.Desktop"
        d:DataContext="{d:DesignInstance Type=vm:CashAccountEditViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        Title="{Binding Title}" Width="560" SizeToContent="Height" MinWidth="500" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="White" FontFamily="Segoe UI">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="B2V" />
            <local:InverseBooleanToVisibilityConverter x:Key="InverseB2V" />
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Margin="18">
        <Border DockPanel.Dock="Top" Background="{StaticResource R3.Surface.Alt.Brush}" BorderBrush="{StaticResource R3.Border.Brush}" BorderThickness="1" CornerRadius="7" Padding="12" Margin="0,0,0,10"
                Visibility="{Binding IsNew, Converter={StaticResource InverseB2V}}">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"><TextBlock FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold"><Run Text="{Binding Code}"/><Run Text="   "/><Run Text="{Binding Name}"/></TextBlock><TextBlock Text="{Binding CurrencyCode}" Foreground="{StaticResource R3.Text.Secondary.Brush}" Margin="0,5,0,0"/></StackPanel>
                <StackPanel Grid.Column="1"><TextBlock Text="Giriş" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/><TextBlock Text="{Binding TotalIn, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="Çıkış" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/><TextBlock Text="{Binding TotalOut, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold"/></StackPanel>
                <StackPanel Grid.Column="3"><TextBlock Text="Bakiye" Foreground="{StaticResource R3.Text.Secondary.Brush}" FontSize="{StaticResource R3.Font.Grid}"/><TextBlock Text="{Binding Balance, StringFormat=N2}" FontSize="{StaticResource R3.Font.Title}" FontWeight="SemiBold" Foreground="{StaticResource R3.Accent.Brush}"/></StackPanel>
            </Grid>
        </Border>
        <StackPanel DockPanel.Dock="Bottom" Margin="0,14,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Firebrick" TextWrapping="Wrap" Margin="0,0,0,8" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Vazgeç" IsCancel="True" Width="78" Height="30" Padding="10,4" Margin="0,0,7,0" />
                <ui:Button Content="Kaydet" Command="{Binding SaveCommand}" Width="90" Height="30" Padding="10,4" Appearance="Primary" IsDefault="True" />
            </StackPanel>
        </StackPanel>
        <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel>
            <TextBlock Text="Kasa Kodu *" Margin="0,0,0,5"/><ui:TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" MaxLength="30"/>
            <TextBlock Text="Kasa Adı *" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="200"/>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"><TextBlock Text="Şirket *" Margin="0,0,0,5"/><ComboBox ItemsSource="{Binding Companies}" DisplayMemberPath="Ad" SelectedValuePath="Id" SelectedValue="{Binding CompanyId}" Padding="8" IsEnabled="False"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="Şube *" Margin="0,0,0,5"/><ComboBox ItemsSource="{Binding Branches}" DisplayMemberPath="Ad" SelectedValuePath="Id" SelectedValue="{Binding BranchId}" Padding="8"/></StackPanel>
            </Grid>
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"><TextBlock Text="Kasa Tipi *" Margin="0,0,0,5"/><ComboBox ItemsSource="{Binding CashAccountTypes}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding CashAccountType}" Padding="8"/></StackPanel>
                <StackPanel Grid.Column="2"><TextBlock Text="Para Birimi *" Margin="0,0,0,5"/><ComboBox ItemsSource="{Binding Currencies}" DisplayMemberPath="Kod" SelectedValuePath="Kod" SelectedValue="{Binding CurrencyCode}" Padding="8"/></StackPanel>
            </Grid>
            <ui:ToggleSwitch Content="Negatif Bakiyeye İzin Ver" IsChecked="{Binding AllowNegativeBalance}" Margin="0,16,0,0"/>
            <ui:ToggleSwitch Content="Aktif" IsChecked="{Binding IsActive}" Margin="0,10,0,0"/>
            <TextBlock Text="Açıklama" Margin="0,10,0,5"/><ui:TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="60"/>
        </StackPanel></ScrollViewer>
    </DockPanel>
</Window>
